# Database Configuration
# MongoDB connection string
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
# PRODUCTION: Use MongoDB Atlas and add your Vercel deployment IP to Atlas Network Access
MONGO_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/your-database?retryWrites=true&w=majority

# NextAuth Configuration
# Base URL of your application
# DEVELOPMENT: http://localhost:3000
# PRODUCTION: https://task-management-eight-bay-63.vercel.app
NEXTAUTH_URL=http://localhost:3000

# NextAuth Secret - MUST be changed in production
# Generate with: openssl rand -base64 32
# NEVER commit the real secret to version control
# PRODUCTION: Generate a strong secret and store it in Vercel environment variables
NEXTAUTH_SECRET=your-super-secret-key-change-this-in-production-min-32-chars

# Application Environment
# DEVELOPMENT: development
# PRODUCTION: production
NODE_ENV=development
